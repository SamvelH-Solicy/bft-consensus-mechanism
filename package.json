{
  "name": "bft-consensus-mechanism",
  "version": "1.0.0",
  "description": "This repository contains an implementation of a simplified **Byzantine Fault Tolerance (BFT)** consensus mechanism. It simulates validators reaching consensus on proposed blocks in a blockchain-like system, with a focus on state transitions, message broadcasting, and vote collection.",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@types/events": "^3.0.3",
    "events": "^3.3.0"
  }
}
